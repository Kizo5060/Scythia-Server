package com.ruseps.world.content.instance_manager;

import com.ruseps.world.entity.impl.player.Player;

public class InstanceButtons {

	public static boolean isInstanceButton(Player player, int buttonsId) {
		switch (buttonsId) {
		case -6816:
			if (InstanceNpcs.values().length >= 1) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[0]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
		case -6815:
			if (InstanceNpcs.values().length >= 2) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[1]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
		case -6814:
			if (InstanceNpcs.values().length >= 3) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[2]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
		case -6813:
			if (InstanceNpcs.values().length >= 4) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[3]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
		case -6812:
			if (InstanceNpcs.values().length >= 5) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[4]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
		case -6811:
			if (InstanceNpcs.values().length >= 6) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[5]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
		case -6810:
			if (InstanceNpcs.values().length >= 7) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[6]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
		case -6809:
			if (InstanceNpcs.values().length >= 8) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[7]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
			
		case -6808:
			if (InstanceNpcs.values().length >= 9) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[8]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
			
		case -6807:
			if (InstanceNpcs.values().length >= 10) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[9]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
			
		case -6806:
			if (InstanceNpcs.values().length >= 11) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[10]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
			
		case -6805:
			if (InstanceNpcs.values().length >= 12) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[11]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
			
		case -6804:
			if (InstanceNpcs.values().length >= 13) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[12]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
			
		case -6803:
			if (InstanceNpcs.values().length >= 14) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[13]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
			
		case -6802:
			if (InstanceNpcs.values().length >= 15) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[14]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
			
		case -6801:
			if (InstanceNpcs.values().length >= 16) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[15]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
			
		case -6800:
			if (InstanceNpcs.values().length >= 17) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[16]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
			
		case -6799:
			if (InstanceNpcs.values().length >= 18) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[17]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
			
		case -6798:
			if (InstanceNpcs.values().length >= 19) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[18]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
			
		case -6797:
			if (InstanceNpcs.values().length >= 20) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[19]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
			
		case -6796:
			if (InstanceNpcs.values().length >= 21) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[20]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
			
		case -6795:
			if (InstanceNpcs.values().length >= 2) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[21]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
			
		case -6794:
			if (InstanceNpcs.values().length >= 23) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[22]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
			
		case -6793:
			if (InstanceNpcs.values().length >= 24) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[23]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
			
		case -6792:
			if (InstanceNpcs.values().length >= 25) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[24]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
			
		case -6791:
			if (InstanceNpcs.values().length >= 26) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[25]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
			
		case -6790:
			if (InstanceNpcs.values().length >= 27) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[26]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
			
		case -6789:
			if (InstanceNpcs.values().length >= 28) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[27]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
			
		case -6788:
			if (InstanceNpcs.values().length >= 29) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[28]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
			
		case -6787:
			if (InstanceNpcs.values().length >= 30) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[29]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
			
		case -6786:
			if (InstanceNpcs.values().length >= 31) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[30]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
			
		case -6785:
			if (InstanceNpcs.values().length >= 32) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[31]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
			
		case -6784:
			if (InstanceNpcs.values().length >= 33) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[32]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
			
		case -6783:
			if (InstanceNpcs.values().length >= 34) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[33]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
			
		case -6782:
			if (InstanceNpcs.values().length >= 35) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[34]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
			
		case -6781:
			if (InstanceNpcs.values().length >= 36) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[35]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
			
		case -6780:
			if (InstanceNpcs.values().length >= 37) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[36]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
			
		case -6779:
			if (InstanceNpcs.values().length >= 37) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[37]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
			
		case -6778:
			if (InstanceNpcs.values().length >= 38) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[38]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
			
		case -6777:
			if (InstanceNpcs.values().length >= 39) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[39]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
			
		case -6776:
			if (InstanceNpcs.values().length >= 40) {
				player.getInstanceManager().setNpcToSelect(InstanceNpcs.values()[40]);
				player.getInstanceManager().refreshNpcDisplayed(player);
			}
			return true;
			
			
			
		case -6582:
			player.getInstanceManager().teleportButtonClick(player);
			return true;
			
		case -6578:
			player.getInstanceManager().timeButtonClick(player);
			return true;
		}
		return false;
	}
}
